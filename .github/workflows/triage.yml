name: Issue Triage
on:
  issues:
    types: [opened, reopened]

permissions:
  issues: write

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: ghassan-gaidi/ai-issue-triage@master
        with:
          labels: "bug,enhancement,question,documentation"
